{
  "name": "promptbetter",
  "version": "0.1.0",
  "description": "Claude Code prompt rewriting hook CLI",
  "type": "module",
  "bin": {
    "promptbetter": "dist/bin/promptbetter.js"
  },
  "scripts": {
    "prepare": "npm run build",
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "npm run build && node --test dist/test/**/*.test.js"
  },
  "engines": {
    "node": ">=20.0.0"
  },
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^22.13.10",
    "typescript": "^5.9.3"
  }
}
